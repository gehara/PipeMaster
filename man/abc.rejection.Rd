% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abc.rejection.R
\name{abc.rejection}
\alias{abc.rejection}
\title{ABC Rejection Algorithm}
\usage{
abc.rejection(
  reftable,
  observed,
  param.cols,
  tol = 0.01,
  distance = c("sd", "mad", "mahalanobis"),
  pls = FALSE,
  n.pls = 20L,
  prior = NULL,
  verbose = TRUE
)
}
\arguments{
\item{reftable}{data.frame or character path — reference table from
\code{sim.sumstat()} or \code{sim.sumstats()} containing parameter
and summary statistic columns. If a file path, read with
\code{read.table(header = TRUE)}.}

\item{observed}{numeric vector or 1-row data.frame — observed summary
statistics.}

\item{param.cols}{character vector — names of parameter columns.}

\item{tol}{numeric — tolerance (acceptance fraction, default 0.01).}

\item{distance}{character — distance metric: \code{"sd"} (default),
\code{"mad"}, or \code{"mahalanobis"}.}

\item{pls}{logical — if TRUE, project summary statistics into PLS
component space before computing distances (default FALSE).
Recommended when the number of statistics is large (>100).}

\item{n.pls}{integer — number of PLS components to retain (default 20).
Ignored if \code{pls = FALSE}.}

\item{prior}{data.frame or NULL — prior samples for plotting (optional).}

\item{verbose}{logical — print progress (default TRUE).}
}
\value{
An object of class \code{"posterior"} with:
\describe{
  \item{point_estimate}{weighted mean of accepted samples (weight = 1/distance)}
  \item{abc}{matrix of accepted parameter samples}
  \item{abc_distance}{distance metric used}
  \item{prior}{prior samples from the reftable}
  \item{param_names}{parameter column names}
}
}
\description{
Performs approximate Bayesian computation (ABC) by rejection sampling.
Accepts the closest simulations to the observed data based on Euclidean
distance in summary statistic space, optionally normalized by standard
deviation, median absolute deviation, or Mahalanobis distance.
}
