% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.search.R
\name{update.priors.from.grid}
\alias{update.priors.from.grid}
\title{Update model priors from grid search results}
\usage{
\method{update}{priors.from.grid}(model, grid.results, tol = 0.05, padding = 0.1)
}
\arguments{
\item{model}{A PipeMaster model object (with \code{model$flags}).}

\item{grid.results}{A data.frame returned by \code{\link{grid.search}}, with
parameter columns and a \code{distance} column.}

\item{tol}{Fraction of top grid cells to use (default 0.05 = top 5\%).}

\item{padding}{Fractional margin in log space beyond min/max of top cells
(default 0.1).}
}
\value{
The model with updated (narrowed) priors.
}
\description{
Narrows model priors to the productive parameter region identified
  by \code{\link{grid.search}}. For each parameter, the new uniform prior spans
  the min–max range of the top grid cells (by distance), with optional padding
  in log space. This is the recommended step between grid search and full ABC.
}
\examples{
\dontrun{
result <- grid.search(model = my_model, observed = obs,
                      n_points = 10, n_reps = 10, ncores = 4)
my_model <- update.priors.from.grid(my_model, result, tol = 0.05, padding = 0.1)
}
}
\author{
Marcelo Gehara
}
