% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OOD.projection.R
\name{OOD.outliers}
\alias{OOD.outliers}
\title{Plot simulated distributions for OOD outlier statistics}
\usage{
OOD.outliers(
  ood_result,
  observed,
  reftable,
  max_stats = 24,
  ncol = 4,
  pdf_file = NULL,
  pdf_width = 12,
  pdf_height = 9
)
}
\arguments{
\item{ood_result}{output of \code{OOD.pretrain()} or \code{OOD.posttrain()}.}

\item{observed}{numeric vector or 1-row data.frame of observed stats, same
as passed to \code{OOD.pretrain()} / \code{OOD.posttrain()}.}

\item{reftable}{data.frame of simulated stats (the reference table).}

\item{max_stats}{integer — maximum number of outlier stats to plot
(default 24). Most extreme first.}

\item{ncol}{integer — number of histograms per row in the grid (default 4).}

\item{pdf_file}{character or NULL — if provided, writes the plot to this
PDF file. Otherwise plots to the active device.}

\item{pdf_width, pdf_height}{numeric — PDF dimensions if \code{pdf_file} given.}
}
\value{
Invisibly returns the outlier data.frame (sorted by extremity, truncated).
}
\description{
Small-multiples diagnostic: one histogram per outlier statistic showing the
simulation distribution with the observed value overlaid as a vertical line.
Complements the meta-histogram in \code{OOD.pretrain()} /
\code{OOD.posttrain()} by letting the user inspect each problematic stat
in detail.
}
\details{
Outliers are sorted by extremity (\code{max(percentile, 1 - percentile)})
and truncated to \code{max_stats}.
}
