% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.tools.R
\name{plot.2D.sfs}
\alias{plot.2D.sfs}
\title{Plot a 2D Joint Site Frequency Spectrum}
\usage{
\method{plot}{`2D.sfs`}(
  sfs,
  pop_sizes = NULL,
  pop_names = c("Pop 1", "Pop 2"),
  folded = FALSE,
  log = TRUE,
  col = NULL,
  main = "Joint SFS",
  ...
)
}
\arguments{
\item{sfs}{A matrix (joint SFS) or numeric vector (flattened joint SFS from sim.sfs output).}

\item{pop_sizes}{Integer vector of length 2 with per-population sample sizes. Required when sfs is a vector.}

\item{pop_names}{Character vector of length 2 with population labels. Default is c("Pop 1", "Pop 2").}

\item{folded}{Logical. If TRUE, folds the SFS by combining entries (i,j) with their complement (n1-i, n2-j). Default is FALSE.}

\item{log}{Logical. If TRUE (default), uses log10 color scale.}

\item{col}{Color palette vector. Default uses a 256-color YlOrRd palette.}

\item{main}{Plot title. Default is "Joint SFS".}

\item{...}{Additional arguments passed to image().}
}
\description{
Plots a 2D joint SFS as a heatmap with log10 color scale and colorbar,
             following the standard convention used by dadi and moments.
             Accepts either a matrix or a flattened vector (as output by sim.sfs for 2-population models).
             Zero entries are masked (shown as white). Pop 1 is on the y-axis and Pop 2 on the x-axis.
}
\examples{
\dontrun{
# From sim.sfs output
sim.data <- read.table("SIM_SFS_model.txt", header = TRUE)
sfs_cols <- grep("^sfs_", colnames(sim.data))

# Plot a single replicate (unfolded)
sfs_vec <- as.numeric(sim.data[1, sfs_cols])
plot.2D.sfs(sfs_vec, pop_sizes = c(64, 14))

# Plot folded SFS
plot.2D.sfs(sfs_vec, pop_sizes = c(64, 14), folded = TRUE)

# Plot average across replicates
avg_sfs <- colMeans(sim.data[, sfs_cols])
plot.2D.sfs(avg_sfs, pop_sizes = c(64, 14),
            pop_names = c("A. piscivorus", "A. conanti"))
}
}
\author{
Marcelo Gehara
}
