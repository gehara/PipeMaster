% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_h_menu.R
\name{hModel}
\alias{hModel}
\title{Create an hModel object for hierarchical codemographic analysis}
\usage{
hModel(
  Ne.prior,
  NeA.prior,
  time.prior,
  gene.prior,
  coexp.prior,
  var.zeta = "FREE",
  th = 0,
  mu.rates,
  alpha = FALSE,
  phylip_paths = NULL
)
}
\arguments{
\item{Ne.prior}{Data frame (nspecies x 4): species name, distribution, min Ne, max Ne.}

\item{NeA.prior}{Data frame (nspecies x 4): species name, distribution, min ancestral Ne ratio, max ancestral Ne ratio.}

\item{time.prior}{Data frame (nspecies x 5): species name, distribution, min time, max time, generation time.}

\item{gene.prior}{List of data frames (one per species). Each data frame is a locfile with columns: id, n, pop, length.}

\item{coexp.prior}{Numeric vector of length 2: c(min, max) uniform prior on coexpansion time.}

\item{var.zeta}{"FREE" or a numeric value (0-1). Proportion of coexpanding species.}

\item{th}{Numeric. Threshold for minimum time difference between Ts and population-specific times. Default 0.}

\item{mu.rates}{List of per-species mutation rate distribution specs. Each element is a list like
\code{list("rnorm", NA, 1e-8, 1.5e-9)}. Must have one element per species.}

\item{alpha}{Logical. If TRUE, demographic changes are exponential. If FALSE, sudden. Default FALSE.}

\item{phylip_paths}{Optional named list mapping species names to file paths of multi-locus PHYLIP files used to build gene.prior. Stored for reference and GUI template reload. Default NULL.}
}
\value{
An object of class \code{"hModel"}.
}
\description{
Bundles all priors and settings needed by \code{sim.coexp.ngs()} into
  a single S3 object of class \code{"hModel"}.
}
\examples{
\dontrun{
Ne   <- data.frame(species=c("sp1","sp2"), dist="uniform", min=10000, max=500000)
NeA  <- data.frame(species=c("sp1","sp2"), dist="uniform", min=0.01, max=10)
Time <- data.frame(species=c("sp1","sp2"), dist="uniform", min=10000, max=500000, gen=1)
gp   <- list(locfile_sp1, locfile_sp2)  # data.frames with columns: id, n, pop, length
mu   <- list(list("rnorm", NA, 1e-8, 1.5e-9),
             list("rnorm", NA, 1e-8, 1.5e-9))
hm   <- hModel(Ne, NeA, Time, gp, coexp.prior=c(10000,500000), mu.rates=mu)
sim.coexp.ngs(hm, nsims=100)
}
}
