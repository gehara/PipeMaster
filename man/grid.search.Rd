% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.search.R
\name{grid.search}
\alias{grid.search}
\title{Grid search for parameter space exploration}
\usage{
grid.search(
  model,
  observed,
  grid = NULL,
  n_points = 10,
  n_reps = 10,
  type = c("sumstat", "sfs"),
  use.alpha = FALSE,
  mu.rates = NULL,
  ncores = 1,
  one.snp = FALSE,
  folded = FALSE,
  method = c("stochastic", "expected"),
  monomorphic = FALSE
)
}
\arguments{
\item{model}{A model object built by main.menu.gui() with data structure set
via get.data.structure().}

\item{observed}{Named numeric vector of observed summary statistics or SFS bins.}

\item{grid}{Optional data.frame with columns named after model parameters.
If NULL, auto-generated on log scale from model priors using \code{n_points}.}

\item{n_points}{Number of grid points per parameter when auto-generating grid.
Default is 10.}

\item{n_reps}{Number of replicates per grid cell. Default is 10.}

\item{type}{Character. Either \code{"sumstat"} or \code{"sfs"}. Default is \code{"sumstat"}.}

\item{use.alpha}{Logical. If TRUE the most recent population size change will be
exponential. Default is FALSE.}

\item{mu.rates}{Mutation rate specification. Either a single numeric value (fixed rate
for all loci), a list specifying a distribution (e.g.,
\code{list("rtnorm", n, mean, sd, lower)}), or NULL to sample from model priors.
Default is NULL.}

\item{ncores}{Number of cores for parallel execution via mclapply. Default is 1.}

\item{one.snp}{Logical. For SFS type: if TRUE, one SNP per locus. Default is FALSE.}

\item{folded}{Logical. For SFS type: if TRUE, fold the SFS. Default is FALSE.}

\item{method}{Character. For SFS type: \code{"stochastic"} or \code{"expected"}.
Default is \code{"stochastic"}.}

\item{monomorphic}{Logical. For SFS type: if TRUE, prepend monomorphic bin.
Default is FALSE.}
}
\value{
A data.frame with grid parameter columns and a \code{distance} column,
  sorted by distance (ascending). Has attributes \code{"simulated"} (matrix of all
  simulated values) and \code{"observed"} (matched observed vector).
}
\description{
Evaluates a grid of parameter combinations by simulating under each
  combination and computing the distance to observed data. Useful for identifying
  productive parameter regions before running full ABC reference tables.
}
\examples{
\dontrun{
# Load example model
data("A_piscivorus", package = "PipeMaster")

# Compute observed stats
obs <- obs.sumstat.ngs(model = Is_Ap,
                       path.to.phylip = "my_data.phy",
                       pop.assign = pop_assign)

# Grid search with auto-generated grid (10 points per parameter)
result <- grid.search(model = Is_Ap, observed = obs[1, ],
                      n_points = 5, n_reps = 5,
                      mu.rates = 1e-8, ncores = 4)

# Inspect top parameter combinations
head(result)

# Custom grid
my_grid <- expand.grid(
  Ne0.pop1 = 10^seq(3, 6, length.out = 5),
  Ne0.pop2 = 10^seq(3, 6, length.out = 5),
  join1    = 10^seq(4, 6, length.out = 5)
)
result2 <- grid.search(model = Is_Ap, observed = obs[1, ],
                       grid = my_grid, n_reps = 10, ncores = 4)
}
}
\author{
Marcelo Gehara
}
