% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observed.sumstat.ngs.R
\name{one.snp.per.locus}
\alias{one.snp.per.locus}
\title{Subsample one SNP per locus from a multi-locus VCF or PHYLIP file}
\usage{
one.snp.per.locus(
  path.to.vcf = NULL,
  path.to.phylip = NULL,
  output,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{path.to.vcf}{Path to input VCF file. Each CHROM value defines a locus.
Exactly one of \code{path.to.vcf} or \code{path.to.phylip} must be provided.}

\item{path.to.phylip}{Path to a multi-locus sequential PHYLIP file.}

\item{output}{Path to output VCF file.}

\item{seed}{Optional integer seed for reproducibility (passed to \code{set.seed}).}

\item{verbose}{Logical. If TRUE prints summary. Default is TRUE.}
}
\value{
Invisibly returns the number of loci written (those with >= 1 seg site).
}
\description{
Reads a multi-locus VCF or PHYLIP file and writes a VCF
             containing exactly one randomly sampled segregating site per
             locus.  Loci with no segregating sites are skipped.  Output is
             always VCF regardless of input format.
             Uses reservoir sampling in C for speed.
}
