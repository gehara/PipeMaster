% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emulator.R
\name{emulator.ABC}
\alias{emulator.ABC}
\title{ABC Rejection Posterior via Neural Emulator}
\usage{
emulator.ABC(
  emulator,
  observed,
  model = NULL,
  prior.bounds = NULL,
  reftable = NULL,
  n_samples = 1e+06,
  tol = 0.01,
  distance = c("euclidean", "mse"),
  verbose = TRUE
)
}
\arguments{
\item{emulator}{list -- output from \code{train.emulator()}.}

\item{observed}{numeric vector or 1-row data.frame -- observed summary statistics.}

\item{model}{PipeMaster model object -- used to extract prior bounds via
\code{get.prior.table()}. Either \code{model} or \code{prior.bounds}
must be provided.}

\item{prior.bounds}{data.frame with columns \code{Parameter}, \code{prior.1},
\code{prior.2} -- alternative to \code{model} for specifying parameter bounds.}

\item{reftable}{optional data.frame -- if provided, extracts prior samples for
the \code{nn.posterior} object (used by \code{plot()} with \code{show_prior}).}

\item{n_samples}{integer -- number of parameter vectors to draw from the prior
(default 1e6).}

\item{tol}{numeric -- fraction of closest samples to retain as posterior
(default 0.01, i.e. top 1 percent).}

\item{distance}{character -- distance metric: \code{"euclidean"} (default) or
\code{"mse"}.}

\item{verbose}{logical -- print progress (default TRUE).}
}
\value{
An object of class \code{"nn.posterior"} with:
\describe{
  \item{point_estimate}{weighted mean of accepted samples (1/distance weights)}
  \item{abc_rejection}{matrix (n_accepted x n_params) of posterior samples}
  \item{prior}{prior samples from reftable (if provided)}
  \item{param_names}{character vector of parameter names}
}
}
\description{
Performs ABC (Approximate Bayesian Computation) rejection sampling using a
trained neural emulator as a fast simulator surrogate. Samples parameter
vectors from the prior, predicts summary statistics through the emulator
ensemble, and retains the closest \code{tol} fraction as approximate
posterior samples.

Returns an \code{nn.posterior} object so that existing \code{summary()},
\code{density()}, and \code{plot()} methods work automatically.
}
