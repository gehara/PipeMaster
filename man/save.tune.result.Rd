% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune.nn.R
\name{save.tune.result}
\alias{save.tune.result}
\title{Save tune.nn Result to Disk}
\usage{
save.tune.result(tune.result, path)
}
\arguments{
\item{tune.result}{list — output from \code{tune.nn()}.}

\item{path}{character — directory path where files will be saved (created if needed).}
}
\description{
Saves the output of \code{tune.nn()} so it can be loaded on another machine.
The keras model is serialized separately using \code{keras::save_model_tf()}.
When multiple models are present (from \code{top_k > 1}), each model is saved
to a separate subdirectory under \code{models/}.
}
