% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.phylip.R
\name{filter.phylip}
\alias{filter.phylip}
\title{Filter and downsample a multi-locus PHYLIP file to match a uniform-sample model}
\usage{
filter.phylip(
  path.in,
  path.out,
  model,
  pop.assign,
  method = c("random", "first"),
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{path.in}{Path to the input PHYLIP file (multi-locus, interleaved).}

\item{path.out}{Path to write the filtered PHYLIP file.}

\item{model}{A model object produced by \code{optimize.sfs.model()}.
Must have uniform per-population sample sizes across all loci.}

\item{pop.assign}{A two-column data frame (or path to a tab/whitespace file
with a header) mapping sample names to population numbers.}

\item{method}{Downsampling method: \code{"random"} (default) picks
\code{target[p]} samples uniformly at random per population per locus;
\code{"first"} picks the first \code{target[p]} matching samples in the
order they appear in the input PHYLIP.}

\item{seed}{Optional integer seed for \code{method = "random"}.}

\item{verbose}{Print a progress bar (default \code{TRUE}).}
}
\value{
The output path, invisibly.
}
\description{
Writes a new PHYLIP file containing only the loci retained by
  \code{optimize.sfs.model()}, with each population downsampled to the
  per-population target size encoded in the model. The output PHYLIP has
  uniform sample sizes across all loci and is suitable for the
  \code{uniform_samples} path of \code{observed.sumstats()}.
}
\author{
Marcelo Gehara
}
