% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observed.sumstat.ngs.R
\name{observed.sumstats}
\alias{observed.sumstats}
\title{Observed summary statistics and SFS from a PHYLIP or VCF file}
\usage{
observed.sumstats(
  model,
  path.to.phylip = NULL,
  path.to.vcf = NULL,
  pop.assign,
  one.snp = FALSE,
  monomorphic = FALSE,
  variable_samples = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{model}{A model object built by the main.menu.gui() function.}

\item{path.to.phylip}{Path to a multi-locus sequential PHYLIP file.
Exactly one of path.to.phylip or path.to.vcf must be provided.}

\item{path.to.vcf}{Path to a VCF file where each CHROM represents a locus.
The VCF should contain phased or unphased diploid genotypes (GT field).
Sample names in pop.assign must match VCF header sample columns.}

\item{pop.assign}{A two-column data frame: sample names in column 1,
population number in column 2.}

\item{one.snp}{Logical. If TRUE, one segregating site is randomly sampled
per locus for the SFS. Default is FALSE.}

\item{monomorphic}{Logical. If TRUE a monomorphic bin is prepended to the SFS.
Default is FALSE.}

\item{verbose}{Logical. If TRUE prints progress messages. Default is TRUE.}
}
\value{
A one-row matrix with columns: summary statistics (4 moments) then SFS bins,
        matching the non-parameter columns of \code{sim.sumstats()}.
}
\description{
Calculates observed summary statistics and the Site Frequency Spectrum
             from a multi-locus sequential PHYLIP file or a VCF file. The output
             columns match \code{sim.sumstats()} (summary statistics followed by
             SFS bins), so the result can be used directly for ABC or emulator inference.
}
\note{
This function does not work on Windows systems.
}
\author{
Marcelo Gehara
}
